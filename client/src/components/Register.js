import React from 'react';
import InteractiveRegister from './InteractiveRegister';

const Register = ({ onLogin }) => {
  return <InteractiveRegister onLogin={onLogin} />;
};

export default Register; 